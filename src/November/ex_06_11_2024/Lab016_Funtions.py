# Functions: Piece of code.
import math
""" 
In-build Functions
input(), print(), max(), type(), id(), pow(), len(), list(), math.cos(), 
range(), math.factorial(), sorted(), int(), str(), float(), bool()

"""

# Functions has 2 steps:
# Defination which is d by def keyword.
# 2nd type of function is user defined.

#Defination
def first_function ():
    print("Hello First function")
# Calling
# first_function()

# We can call the function in any order

def second_function():
    print("First function is inside the second function")
    first_function()


# second_function()
# first_function()

 # We can also call a function within a function
second_function()






























